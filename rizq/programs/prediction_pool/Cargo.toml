[package]
name = "prediction_pool"
version = "0.1.0"
description = "Rizq prediction pool program"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "prediction_pool"

[features]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
cpi = ["no-entrypoint"]
idl-build = ["anchor-lang/idl-build", "anchor-spl/idl-build"]
default = []

[dependencies]
anchor-lang = { version = "0.31.1", features = ["init-if-needed"] }
anchor-spl = "0.31.1"
savings_goal = { path = "../savings_goal", features = ["cpi"] }
